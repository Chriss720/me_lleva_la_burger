name: PR Welcome

on:
  pull_request:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Welcome Comment
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: "Hola @${{ github.actor }}! 👋\n\nGracias por tu contribución al proyecto **Me Lleva la Burger**. 🍔\n\nNuestros sistemas de CI revisarán tu código en breve. Por favor asegúrate de que todos los tests pasen.\n\n¡Éxito!"
            })
